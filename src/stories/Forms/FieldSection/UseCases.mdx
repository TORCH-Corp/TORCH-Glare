import { Meta, Title, Source } from '@storybook/addon-docs';
import { FieldSection} from '@/index'
import * as FieldSectionStories from './FieldSection.stories.tsx';

<Title>FieldSection Component</Title>
<Meta of={FieldSectionStories} />

This is the Glare FieldSection component, a versatile input solution that supports various configurations such as labels, icons, dropdowns, and action elements like buttons and badges. It is designed for flexibility with options for size, style, negative theming, and error handling.


## Use Cases

### Form Use

  <div style={{ display: 'flex', flexDirection: 'column',margin:"30px 0" }}>
        <FieldSection name={'example'} label={'Employee Name'} required_label={'*'} />
        <FieldSection name={'example2'} label={'BirthDate'} required_label={'*'} />
        <FieldSection name={'example3'} label={'Region'} required_label={'*'} />
  </div>
