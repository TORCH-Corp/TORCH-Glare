import { Meta, Title, Source } from '@storybook/blocks';
import { InputField,LoginButton } from '../../../lib/index.ts';
import * as LoginButtonStories from './LoginButton.stories.tsx';

<Title>LoginButton Component</Title>
<Meta of={LoginButtonStories} />

  The LoginButton component is a customizable button that can be used for login actions. It supports all standard HTML button attributes and can be styled using the `className` prop.


## Use Cases

### Login Use

  <div style={{ maxWidth:"400px", display: 'flex',gap:"8px", flexDirection: 'column',margin:"30px 0" }}>
        <InputField name={'example'} type='email' label={'Email'}  />
        <InputField name={'example2'} type="password" label={'Password'}  />
        <LoginButton>Login</LoginButton>
  </div>
