import { Datepicker ,InputField,Label} from '@/index'
import * as DatepickerStories from './DatePicker.stories.tsx';
import { Meta, Title, Source } from '@storybook/blocks';

<Title>DatePicker Component Use Cases</Title>
<Meta of={DatepickerStories}/>

This is the Glare Datepicker component with various styles, sizes, and configurations, here are some use cases.

## Use Cases

### Form Use Case

    <div style={{ maxWidth:"400px", display: 'flex', flexDirection: 'column', margin: "30px 0",gap:"10px" }}>
      <InputField name={'example'} label={'Employee Name'} required_label={'*'} />
      <Label name={"undefined"} label='Birthday' required_label={'*'} >
        <Datepicker component_style="presentation" name={'example2'} placeholder={''} />
      </Label>
    </div>