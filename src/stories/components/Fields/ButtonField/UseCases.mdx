import { Meta, Title, Source } from '@storybook/addon-docs';
import { ButtonField, Button } from '@/index'
import * as ButtonFieldStories from './ButtonField.stories.tsx';

<Title>ButtonField Component</Title>
<Meta of={ButtonFieldStories} />

This is the Glare ButtonField component, a flexible container designed to optionally include dividers, allowing for distinct visual separation in UI designs.

## Use Cases

### Navigation Use
    <div style={{ maxWidth: "400px", display: 'flex',justifyContent:"space-between", margin: "30px 0", gap: "10px" }}>        
    <ButtonField with_divider className="custom-style">
        <Button component_size="L" left_icon={<i className="ri-mail-download-line"></i>} />
        <Button component_size="L" left_icon={<i className="ri-bookmark-fill"></i>} />
        <Button component_size="L" left_icon={<i className="ri-reply-all-fill"></i>} />
    </ButtonField>
    </div>