import { Counter } from '../../../lib/index.ts'
import * as CounterStories from './Counter.stories.tsx';
import { Meta, Title, Source } from '@storybook/blocks';

<Title>Counter Component</Title>
  This is the Glare Counter component.

<Meta of={CounterStories} />

### Default Counter
<Counter label={5} />
<Source code={`<Counter label={5} />`} />

## Usage

```jsx
import { Counter } from 'torch-glare';

const App = () => (
  <div>
    <Counter label={42} />
  </div>
);

export default App;
```