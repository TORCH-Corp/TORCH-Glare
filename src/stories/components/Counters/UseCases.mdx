import { ButtonField,IconButton} from '../../../lib';
import * as CounterStories from './Counter.stories.tsx';
import { Meta, Title, Source } from '@storybook/blocks';

<Title>Counter Component Use Case</Title>
<Meta of={CounterStories}/>

  This is the Glare Counter component. here are some use cases

## Use Cases
### Navigation Use Case
    <div style={{padding:"10px", backgroundColor:"black", display: 'flex', flexDirection: "column", justifyContent: "center", alignItems: "center", gap: "10px", maxWidth: "300px" }}>
      <ButtonField with_divider>
        <IconButton icon={<i className="ri-notification-3-fill"></i>} count={6} />
        <IconButton icon={<i className="ri-home-2-fill"></i>} />
        <IconButton icon={<i className="ri-mail-add-line"></i>} count={10}/>
      </ButtonField>
    </div>