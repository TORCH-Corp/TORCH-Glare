import { Alert,FieldSection } from '@/';
import * as AlertStories from './Alert.stories.tsx';
import { Meta, Title,  Source } from '@storybook/blocks';

<Title>Alert Component Use Cases</Title>
<Meta of={AlertStories}/>

The Glare Alert component is used to display different types of alert messages with varying styles. here is some use cases.

## Use Cases

### Form Use
  In this case, we have an employee registration form, and we are using the Alert component to notify the user that the full employee name is required in this field.

  <div style={{ display: 'flex', flexDirection: 'column',margin:"30px 0" }}>
        <FieldSection name={'example'} label={'Employee Name'} warning_label='You must enter Full Employee Name' required_label={'*'} />
        <FieldSection name={'example2'} label={'BirthDate'} required_label={'*'} />
        <FieldSection name={'example3'} label={'Region'} required_label={'*'} />
  </div>




