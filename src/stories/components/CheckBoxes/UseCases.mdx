import { Label,CheckboxLabel } from '../../../lib';
import * as  CheckboxLabelStories from './CheckboxLabel.stories.tsx';
import { Meta, Title } from '@storybook/blocks';

<Title>Button Component</Title>
<Meta of={CheckboxLabelStories}/>

This is the Glare Button component with different styles, sizes, and icons, and here are some use cases.

## Use Cases

#### Submit Use Case

In this example, we are using the CheckboxLabel to select colors.

    <div style={{ display: 'flex', flexDirection: "column", justifyContent: "center", alignItems: "flex-start", gap: "10px", maxWidth: "300px" }}>
      <Label name={"example"} label='Pick a Color' />
      <CheckboxLabel label='Red' check_box_name={'one'} />
      <CheckboxLabel label='Green' check_box_name={'two'} />
      <CheckboxLabel label='Yellow' check_box_name={'three'} />
      <CheckboxLabel label='Blue' check_box_name={'four'} />
    </div>
